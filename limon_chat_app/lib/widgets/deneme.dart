



// Import the necessary Firebase libraries.
/* import 'package:firebase_auth/firebase_auth.dart';

class LoginAndRegister {
  // Get an instance of the Firebase Auth SDK.
  final FirebaseAuth auth = FirebaseAuth.instance;

  // Creates a new user with email and password and returns UserCredential if successful.
  Future<UserCredential> createUserWithEmailAndPassword(
      String email, String password) async {
    return await auth.createUserWithEmailAndPassword(
        email: email, password: password);
  }

  // Signs in the user using email and password and returns UserCredential if successful.
  Future<UserCredential> signInWithEmailAndPassword(
      String email, String password) async {
    return await auth.signInWithEmailAndPassword(
        email: email, password: password);
  }

  // Signs out the user and returns true if successful.
  Future<bool> signOut() async {
    await auth.signOut();
    return true;
  }
}


 */


